# The Heading

> The one thing to push right now. Surfaced first by the Ratchet each session.
> Keep it to a glance; update on every RECORD. (Volatile — the *aim*. The ledger
> is the accumulated memory.)

**Binding constraint right now:** The efficacy claim that sells the whole thing
sits at **TESTED** on informal n=5 trials, not **OBSERVED** — and the new hardening
(Card 0002) is **REASONED**, not measured. The harness to fix both now exists
(`bench/` + `/prove-it`); the constraint is that it hasn't been *run*.

**Next tasks** *(ranked by Priority = Alignment × Leverage × InfoValue ÷ Cost)*

| # | Task | Leverage | InfoValue | Cost | why it's next |
|---|------|----------|-----------|------|---------------|
| ① | Run `/prove-it batch` — fully-blind A/B across `bench/briefs.md`; record in `bench/results.md` + Card 0001 | H | H | M | climbs the one debt the whole claim rests on; the harness is built, just unrun |
| ② | From the same run, read the **persona ablation** (B vs C) and **ceremony tax** (easy slice) | H | H | L | tells us whether to *cut* the personas — the framework's own "best part is no part" |
| ③ | Verify one integration install end-to-end on a live node (OpenClaw — near-1:1) | M | H | M | turns the install shells from REASONED → TESTED |
| ④ | Taste pass on top-of-funnel density (README + site first screen) | M | M | L | the five-second gate, applied to our own front door |

**Riskiest assumption under test now:** that the framework's win generalizes beyond
5 informal trials — *and* that the new mechanisms improve outcomes rather than just
adding ceremony. Task ① is the cheapest falsifier for both.

**WIP on the critical path:** 1 — finish ① before starting ②/③.

<!-- Founder Call: the Alignment ranking encodes taste. Founder confirms true north. -->
