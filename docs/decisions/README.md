# Decision Ledger — The Ritual

> The product's **retained memory** — re-injected every session by the Ratchet
> (`framework/07-the-ratchet.md`) so gains compound instead of leaking. Kept tight
> and current; read in full at session start, so every line earns its place. The
> individual Gate Cards in this folder are the history; this is the living summary.

The rule: **build on the patterns, pay the open debts, never re-open a kill or a
falsified claim.**

> Current aim lives in `heading.md` (surfaced first by the Ratchet). This is the
> accumulated memory; the Heading is where the vector points right now.

---

## Patterns that keep passing (promote to defaults)

- **Wire into the platform's native auto-load slot — never rely on the agent
  volunteering.** Real portability = install into the file a platform already
  reads each session (CLAUDE.md, AGENTS.md, SOUL.md, project instructions), not a
  paste-when-you-remember block. *(integrations/)*
- **Isolation manufactures real divergence; the Weave captures it through the
  gates.** Minds that can't see each other can't converge — then synthesize one
  whole, with provenance. *(framework/10, the Simulation)*
- **Confident ≠ fabricated.** Present real numbers at their honest rung with the
  method visible. Rigor reads as confidence; disclaimers read as doubt. *(Card 0001 · the benchmark section)*
- **Never fake a rung — even in marketing.** The storefront is held to the same
  evidence ladder as the framework. *(Card 0001)*
- **Turn vibes into mechanisms.** The framework is strongest where it's mechanical
  (the Ratchet, the isolated Simulation) and weakest where it's faith (self-grading,
  personas, asserted right-sizing). Prefer a check over more prose telling the model
  to behave: cite-or-downgrade, the isolated Adversary, the self-test harness. *(Card 0002)*
- **The grader can't be the graded.** Red-team from a *fresh, isolated* context
  (`/adversary`), not a self-pass — a model defending its own card mostly passes. *(Card 0002)*

## Open debts (owed to reality)

| Debt | Gate / Card | Evidence owed | Owner |
|------|-------------|---------------|-------|
| Framework efficacy at TESTED, not OBSERVED | Card 0001 · Feasible/Desirable | a larger, **fully-blind** A/B + **real users** judging live decisions — run `/prove-it` | founder |
| The hardening changes are claimed, not measured | Card 0002 · Desirable | run `bench/` to show the Adversary + cite-or-downgrade change what ships, and the persona ablation result | founder/eng |
| Integration installs unproven on live nodes | integrations/ (openclaw, hermes, cowork) | run each install against a real instance → REASONED → TESTED | founder/eng |
| `/adversary` + `/prove-it` unrun end-to-end | Card 0002 · Feasible | spawn the sub-agents in a live session once | founder/eng |

## Kills & falsifieds (do not re-open)

| What | Verdict | Why (one line) | Card |
|------|---------|----------------|------|
| Benchmark copy that undersold itself (disclaimers, "blind-ish," a 0% null bar) | KILLED | honesty ≠ self-deprecation; it read as a bunk product | — |
| The "paste-it-when-you-remember" universal integration | KILLED | not real compatibility; replaced by native-slot installs per platform | — |

---

<!-- The hook reads this file verbatim each session. Prune ruthlessly: a stale
     ledger teaches every future session the wrong thing. -->
