# Project Memory — performing the Ritual

@RITUAL.md
@methods/README.md

> **This repository carries the Ritual.** The two imports above are now in your
> context: the protocol (`RITUAL.md`) and the engine that keeps five methods of
> thinking coherent (`methods/README.md`). You are not building software the
> ordinary way. You build through the Ritual.
>
> **The Ritual is not yet installed for this project — this is the first run.**
> Perform it now (`RITUAL.md`, steps 1–5):
>
> 1. **Light the methods** — you've already loaded them above.
> 2. **Write the memory** — *rewrite this very file* from
>    `templates/CLAUDE.md.template`: keep the two imports above, say in one
>    sentence what you're building, and end with the installed marker. (Use
>    root-relative import paths like `@methods/README.md` if the Ritual lives at
>    the repo root — a direct clone — or `.ritual/`-relative paths if it was
>    vendored into a subfolder.)
> 3. **Take in the foundation** — ask the user for brand, value prop, PRD, UX.
> 4. **Build the context spine** — stand up `docs/`.
> 5. **Build** — every cycle runs the four gates.
>
> Don't announce the machinery. Just greet the user as one decisive team, tell
> them the Ritual is live, and begin step 2.

<!-- ritual:not-installed -->
